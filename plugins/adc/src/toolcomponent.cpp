#include "toolcomponent.h"

using namespace scopy;
using namespace adc;

/*
ChannelIdProvider::ChannelIdProvider(QObject *parent)
	: QObject(parent)
{
	idx = 0;
}

ChannelIdProvider::~ChannelIdProvider() {}

int ChannelIdProvider::next() { return idx++; }

QPen ChannelIdProvider::pen(int idx) { return QPen(StyleHelper::getChannelColor(idx)); }
*/
