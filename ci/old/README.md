# Old CI Directory

This directory contains deprecated CI scripts from previous build systems. These scripts are preserved for reference but are no longer actively used. They represent the Travis CI era of Scopy's build infrastructure before migration to GitHub Actions and AppVeyor.